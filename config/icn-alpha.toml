# ICN Alpha Node Configuration
# This is node 1 of 2 in the local two-node demo.
# Use with: icnd --config config/icn-alpha.toml

data_dir = "/tmp/icn-alpha"

[network]
listen_addr = "0.0.0.0:7777"
rpc_port = 5601
mdns_enabled = true
bootstrap_peers = []

[observability]
metrics_port = 9100
health_port = 8080
log_level = "info"
