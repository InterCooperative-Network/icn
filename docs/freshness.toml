# ICN Documentation Freshness Tracking
# Section-level tracking for docs/ARCHITECTURE.md
#
# evidence_type: code | test | demo | adr | human-asserted
# truth_class: normative | descriptive | operational
# source: path to generator script (for auto-generated sections only)

[sections."01-what-icn-is"]
title = "What ICN Is"
last_updated = "2026-03-21"
last_verified_by = "matt"
staleness_threshold_days = 120
truth_class = "normative"
depends_on = ["docs/strategy/ADR-001-What-ICN-Is.md", "VISION.md"]
evidence_type = "adr"
notes = "Core thesis. Only changes if the project fundamentally redefines itself."

[sections."02-boundary-conditions"]
title = "Boundary Conditions / Anti-Claims"
last_updated = "2026-03-21"
last_verified_by = "matt"
staleness_threshold_days = 120
truth_class = "normative"
depends_on = ["docs/strategy/ADR-001-What-ICN-Is.md", "docs/design/regulatory-safe-verifiable-state.md"]
evidence_type = "adr"
notes = "Regulatory positioning. Changes only if regulatory landscape shifts."

[sections."03-meaning-firewall"]
title = "The Meaning Firewall"
last_updated = "2026-06-14"
last_verified_by = "freshness-2047"
review_note = "Enforcement mechanism corrected (meaning_firewall.rs ratchets, not 'compiler'); infection tally recomputed from kernel_surface.toml per-crate entries (13 clean / 14 infected / 1 needs-review / 1 extracted) in §17/§19. Checked against icn/crates/icn-core/src/meaning_firewall.rs."
staleness_threshold_days = 60
truth_class = "normative"
depends_on = [
  "docs/architecture/KERNEL_APP_SEPARATION.md",
  "icn/crates/icn-kernel-api/src/lib.rs",
  "kernel_surface.toml",
]
evidence_type = "code"
notes = "Invariants are stable but infection status changes as crates get cleaned."

[sections."04-eight-primitives"]
title = "The Eight Primitives"
last_updated = "2026-06-21"
last_verified_by = "state-truth-refresh"
review_note = "Re-verified 2026-06-21 against current src: the eight primitive definitions (Identity/Authorization/State/Compute/Communication/Time/Coordination/Naming) and their crate mappings hold. Correction made this pass: §4.4 'WASM' overclaimed 'Fuel metering enforced by ICN runtime. Deterministic imports (no system time...)'. Reworded to experimental / non-deterministic to match the code (icn/crates/icn-compute/src/wasm_executor.rs: Engine::default() with 'fuel metering can be enabled later', a coarse saturating_sub(100) per-call counter, and wall-clock std::time::SystemTime::now() host imports) and the already-corrected §11 review_note. CCL interpreter determinism/fuel claims unchanged (verified)."
staleness_threshold_days = 90
truth_class = "normative"
depends_on = ["icn/crates/icn-kernel-api/src/**"]
evidence_type = "code"
notes = "Primitive definitions are stable. Implementation details may shift."

[sections."05-regulatory-architecture"]
title = "Regulatory Architecture"
last_updated = "2026-03-21"
last_verified_by = "matt"
staleness_threshold_days = 120
truth_class = "normative"
depends_on = ["docs/design/regulatory-safe-verifiable-state.md"]
evidence_type = "adr"
notes = "Seven invariants. Changes only if regulatory strategy changes."

[sections."06-identity-cryptography"]
title = "Identity & Cryptography"
last_updated = "2026-06-14"
last_verified_by = "freshness-2047"
review_note = "3-of-5 steward threshold verified (icn-steward gossip.rs/enrollment.rs); keystore migration chain v1->v2->v2.1->v3->v4 verified (icn-identity keystore.rs); unverifiable '88 modules' descriptor removed."
staleness_threshold_days = 45
truth_class = "descriptive"
depends_on = [
  "icn/crates/icn-identity/src/**",
  "icn/crates/icn-crypto-pq/src/**",
  "icn/crates/icn-steward/src/**",
  "icn/crates/icn-zkp/src/**",
]
evidence_type = "test"
notes = "Active development area (SDIS phases). Check when new phases complete."

[sections."07-trust-graph"]
title = "Trust Graph"
last_updated = "2026-06-14"
last_verified_by = "freshness-2047"
review_note = "Verified accurate: trust-class thresholds vs TrustClass::from_score (icn-trust/src/lib.rs); rate limits 10/50/100/200 msg/s vs icn-net/src/rate_limit.rs."
staleness_threshold_days = 60
truth_class = "descriptive"
depends_on = ["icn/crates/icn-trust/src/**"]
evidence_type = "test"
notes = "Computation algorithm and trust classes are stable."

[sections."08-network-transport"]
title = "Network & Transport"
last_updated = "2026-06-14"
last_verified_by = "freshness-2047"
review_note = "Verified accurate (code-level only): default ports 7777/5601/9100/8080 vs icn-core/src/config/mod.rs and gateway.rs. The 'K3s deployment' runtime claim is NOT source-verifiable and was NOT re-confirmed this pass — see status.toml subsystems.network (NEEDS OPS RE-CONFIRMATION); do not read this section's freshness as attesting the runtime deployment."
staleness_threshold_days = 60
truth_class = "operational"
depends_on = ["icn/crates/icn-net/src/**"]
evidence_type = "demo"
notes = "QUIC stack present in src and ports verified; the 'proven in K3s deployment' runtime claim NEEDS OPS RE-CONFIRMATION (not verified this pass)."

[sections."09-gossip-synchronization"]
title = "Gossip & Synchronization"
last_updated = "2026-06-14"
last_verified_by = "freshness-2047"
review_note = "Verified accurate: vector_clock.rs/bloom.rs/anti_entropy.rs + push/pull handlers + namespace:purpose topics in icn-gossip. '2+ months K3s runtime' downgraded to NEEDS OPS RE-CONFIRMATION (not source-verifiable)."
staleness_threshold_days = 60
truth_class = "descriptive"
depends_on = ["icn/crates/icn-gossip/src/**"]
evidence_type = "test"
notes = "Gossip protocol stable since Phase 7."

[sections."10-state-ledger"]
title = "State & Ledger"
last_updated = "2026-06-14"
last_verified_by = "freshness-2047"
review_note = "Verified accurate: double-entry invariant + Merkle-DAG (icn-ledger lib.rs/types.rs); receipt chain GovernanceDecisionReceipt->AllocationReceipt->SettlementIntent->JournalEntry (icn-ledger/src/allocations.rs). Demurrage 'Phase 25' tag (retired numbering) corrected to 'planned, not implemented'."
staleness_threshold_days = 45
truth_class = "descriptive"
depends_on = [
  "icn/crates/icn-ledger/src/**",
  "icn/crates/icn-store/src/**",
]
evidence_type = "test"
notes = "Receipt chain is active development (AllocationReceipt wiring)."

[sections."11-contract-execution"]
title = "Contract Execution (CCL)"
last_updated = "2026-06-14"
last_verified_by = "freshness-2047"
review_note = "Verified: FuelConfig, capability-based security, deterministic bounded expressions, Interpreter (icn-ccl). Corrections: (1) wasmtime/`wasm` feature re-attributed from icn-ccl to icn-compute (where the dep actually lives); (2) WASM path marked experimental — it is NOT held to CCL's fuel/determinism guarantees: icn-compute/src/wasm_executor.rs uses Engine::default() (no wasmtime fuel metering, comment 'fuel metering can be enabled later') + coarse per-call counter, and a wall-clock SystemTime::now() timestamp host import (non-deterministic)."
staleness_threshold_days = 45
truth_class = "descriptive"
depends_on = ["icn/crates/icn-ccl/src/**"]
evidence_type = "test"
notes = "CCL runtime stable but ecosystem immature."

[sections."12-governance"]
title = "Governance"
last_updated = "2026-07-14T22:05:00"
last_verified_by = "lan-rehearsal-deployment"
review_note = "Re-verified 2026-07-14: the 9-state proposal lifecycle (incl. ForceClosed) is unchanged vs enum ProposalState in icn-governance/src/proposal.rs (variants re-counted). Dependency changes since the 2026-06-14 verification: #2055 rand 0.8->0.9 (mechanical) in icn-governance, and the additive build-mode-gated rehearsal surface (#2406) in apps/governance, which mounts /rehearsal/* only under the exact-match Rehearsal build mode and does not alter the lifecycle this section describes. Exact test count still NEEDS RE-DERIVATION (unchanged from prior note)."
staleness_threshold_days = 30
truth_class = "operational"
depends_on = [
  "icn/crates/icn-governance/src/**",
  "icn/apps/governance/src/**",
]
evidence_type = "demo"
notes = "Most actively verified subsystem; demo flow proven. Exact test count NEEDS RE-DERIVATION (prior '547 tests' is stale — see review_note and status.toml governance row); run cargo test for an exact figure before re-asserting."

[sections."13-entity-model"]
title = "Entity Model"
last_updated = "2026-06-14"
last_verified_by = "freshness-2047"
review_note = "Verified accurate: EntityType/EntityKind (icn-entity), coop Formation->Active->Dissolution (icn-coop/handle.rs), CommunityType Geographic/Interest/Solidarity (icn-community/types.rs), deterministic treasury DID derivation (icn-coop)."
staleness_threshold_days = 60
truth_class = "descriptive"
depends_on = [
  "icn/crates/icn-entity/src/**",
  "icn/crates/icn-coop/src/**",
  "icn/crates/icn-community/src/**",
]
evidence_type = "test"
notes = "Entity model fully implements spec. Stable."

[sections."14-federation"]
title = "Federation"
last_updated = "2026-08-14T00:00:00"
last_verified_by = "status-refresh-2026-08-14"
review_note = "Re-verified 2026-08-14 at HEAD 74c832f1 against icn/crates/icn-federation/src/. CORRECTION: the 'no multilateral netting' claim was FALSE and had propagated to three places (this note, the ARCHITECTURE.md section body, and the status.toml federation gap list). netting.rs:1,44 implements NettingEngine, it is exported at lib.rs:73, and it is called from production at clearing_manager.rs:414-420; the real limitation, recorded in source at :412 and previously absent from all three docs, is that it is informational only and does not modify positions. Note the failure mode: the 2026-07-14 pass re-affirmed the prior claim on the basis that no semantic federation change had landed, rather than re-checking source — so a wrong claim survived two verification passes. Also corrected this pass: attestation signing is not installed in production (FederationGossipHandler::set_keypair, gossip.rs:83, has callers only under #[cfg(test)]; init_federation.rs:128-133 never installs a keypair; icn-rpc/src/handler/federation.rs:566-575 stores attestations without .sign()), so announcements are emitted unsigned into a verifier path that expects signatures. Re-affirmed at source: bilateral clearing (clearing.rs, receipt_clearing.rs), dispute resolution still an Option<String> method label (agreement/types.rs:493), and no checkpoint/epoch or bft/byzantine types in this crate (icn-core carries the partition-healing and byzantine integration tests). NOT re-verified: no build, test or runtime execution."
staleness_threshold_days = 30
truth_class = "descriptive"
depends_on = ["icn/crates/icn-federation/src/**"]
evidence_type = "code"
notes = "Known gaps (BFT, netting, dispute resolution). Review when any federation work lands."

[sections."15-distributed-compute"]
title = "Distributed Compute"
last_updated = "2026-06-14"
last_verified_by = "freshness-2047"
review_note = "Verified: still foundation-only — no marketplace/bidding/WorkloadManifest in icn-compute. Updated: cost model now exists (cost.rs, fuel x payment_rate -> credit cost), so 'workers execute for free' qualified."
staleness_threshold_days = 60
truth_class = "descriptive"
depends_on = ["icn/crates/icn-compute/src/**"]
evidence_type = "code"
notes = "Foundation only. Low change velocity expected until Phase 2 Track E."

[sections."16-security"]
title = "Security"
last_updated = "2026-06-14"
last_verified_by = "freshness-2047"
review_note = "Major correction: onion routing now IMPLEMENTED (icn-privacy/src/onion_routing.rs 540 LOC, wired via icn-net handlers/onion.rs + connection.rs dispatch), not 'deferred'. Retired 'Phase 21/22' numbering removed. Three-layer model + rate limits re-verified. NEEDS SME review: onion anonymity threat model."
staleness_threshold_days = 60
truth_class = "operational"
depends_on = [
  "icn/crates/icn-security/src/**",
  "icn/crates/icn-privacy/src/**",
  "icn/crates/icn-net/src/**",
]
evidence_type = "test"
notes = "Three-layer model proven. Privacy features deferred to Phase 20-21."

[sections."17-crate-map"]
title = "Crate Map"
last_updated = "auto"
truth_class = "descriptive"
source = "docs/scripts/generate-index.py"
depends_on = [
  "icn/Cargo.toml",
  "kernel_surface.toml",
  "icn/crates/*/doc.toml",
]
evidence_type = "auto"
notes = "Auto-generated from Cargo.toml + kernel_surface.toml."

[sections."18-institution-primitives"]
title = "Institution Primitives"
last_updated = "2026-07-14T22:05:00"
last_verified_by = "lan-rehearsal-deployment"
review_note = "Re-verified 2026-07-13 (PR docs/vision-current-doctrine): the four institution primitives still compose from the eight kernel primitives as described; section content re-read against the rewritten VISION.md dependency and remains consistent. Changes this pass: the section's starter-bundle heading no longer uses the retired 'Institution-in-a-Box' product name as current framing (historical name annotated, archived vision linked); no primitive/composition change. Prior 2026-06-21 verification (TOML→YAML charter correction) preserved in git history."
staleness_threshold_days = 90
truth_class = "normative"
depends_on = ["VISION.md", "docs/design/institution-in-a-box.md"]
evidence_type = "human-asserted"
notes = "Compositional layer. Changes only when primitives or institution model changes."

[sections."19-implementation-status"]
title = "Implementation Status"
last_updated = "auto"
truth_class = "operational"
source = "docs/scripts/freshness-check.py"
depends_on = [
  "docs/status.toml",
  "kernel_surface.toml",
]
evidence_type = "auto"
notes = "Auto-generated from status.toml + CI results."

[sections."20-reference-index"]
title = "Reference Index"
last_updated = "auto"
truth_class = "descriptive"
source = "docs/scripts/generate-index.py"
depends_on = ["docs/registry.toml"]
evidence_type = "auto"
notes = "Auto-generated from registry.toml."
