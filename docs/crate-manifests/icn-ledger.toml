# ICN Ledger - Documentation Manifest

[crate]
name = "icn-ledger"
layer = "app"
infection_status = "clean"
owner = "core"
doc_entry = "docs/reference/crates/icn-ledger.md"
status = "stable"
exports_surface = true
regulated_terms_allowed = false
primitives = ["ledger", "capability", "identity"]
description = "Ledger and state subsystem; UTXO model, state proofs, auditability"

[documentation]
overview = true
examples = true
safety = true
tests = true
integration = true

[implementation]
test_count = 412
example_count = 18
unsafe_blocks = 8
feature_flags = ["utxo-variant", "bench", "proof-generation"]
dependencies = ["serde", "serde_json", "thiserror", "rocksdb"]
peer_dependencies = ["icn-kernel-api", "icn-crypto-pq", "icn-meaning-firewall"]

[publication]
published_to_crates_io = true
version = "0.5.1"
readme_exists = true
changelog_exists = true
api_stability = "stable"

[verification]
tests_pass = true
lint_clean = true
docs_compile = true
no_forbidden_terms = true
infection_vectors_documented = false
capability_leaks_tested = true

[notes]
"App layer: Uses public kernel-api surface. Directly implements regulatory-safe state."
"Ledger model: UTXO-style immutable state model with commitment proofs."
"No forbidden terms: Does NOT use 'balance', 'wallet', 'transaction' (uses 'capability-transfer')."
"Unsafe blocks (8) relate to serialization and merkle tree proofs - all documented."
"State proofs: Can generate zero-knowledge proofs of state without exposing ledger internals."
"Integration: Works with meaning-firewall for regulatory compliance and audit trails."
