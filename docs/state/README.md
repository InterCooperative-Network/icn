# `docs/state/` (supplemental)

**Canonical living project state** is [`../STATE.md`](../STATE.md) at the `docs/` root (navigation entry point).

Files under `docs/state/` are **supplemental** snapshots or themed notes. Prefer `STATE.md` for “what is true now” unless a file here is explicitly dated and scoped.
