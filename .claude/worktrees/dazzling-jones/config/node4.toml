# ICN Node 4 Configuration (Byzantine Test Node)
data_dir = "/data"

[network]
mdns_enabled = true
listen_addr = "0.0.0.0:5004"
rpc_port = 5604
bootstrap_peers = []
stun_servers = []  # Disabled for Docker testing

[observability]
metrics_port = 9100
health_port = 8080
log_level = "debug"  # More verbose for Byzantine attack analysis
