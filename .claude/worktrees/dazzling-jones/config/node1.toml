# ICN Node 1 Configuration
data_dir = "/data"

[network]
mdns_enabled = true
listen_addr = "0.0.0.0:5001"
rpc_port = 5601
bootstrap_peers = []
stun_servers = []  # Disabled for Docker testing

[observability]
metrics_port = 9100
health_port = 8080
log_level = "info"
