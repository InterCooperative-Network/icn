[package]
name = "icn-encoding"
version.workspace = true
edition.workspace = true
license = "Apache-2.0"
description = "Encoding abstraction layer for ICN serialization (bincode → postcard migration)"

[dependencies]
# Serialization
serde = { workspace = true }
bincode = { workspace = true }
postcard = { workspace = true }

# Error handling
thiserror = { workspace = true }

[dev-dependencies]
serde = { workspace = true }

[lints]
workspace = true
